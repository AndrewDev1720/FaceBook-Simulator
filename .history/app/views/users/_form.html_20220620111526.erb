<div class="container-xl">
    <div class="row justify-content-center">
        <div class="col-10">
            <%=render 'shared/errors'%>
            <%= form_with(model: @user, class:"shadow p-3 mb-3 bg-info rounded", local: true) do |f|%>
                <div class="form-group row">
                    <%= f.label :username, class: "col-2 col-form-label text-white"%> <br/>
                    <div class="col-10"><%= f.text_field(:title, class: "form-control shadow rounded", placeholder: "User name")%></div>
                </div>
                <div class="form-group row">
                    <%= f.label :description, class: "col-2 col-form-label text-light"%> <br/>
                    <div class="col-10"><%= f.text_area:description, rows: 10, class: "form-control shadow rounded", placeholder:"Description of the Article"%></div>
                </div>
                <div class="form-group row justify-content-center"><%= f.submit class:"btn btn-outline-light btn-lg"%></div>
            <%end%>
            <div class="form-group row justify-content-center">
                <%= link_to "Back to Articles List", articles_path, method: :get, class:"btn btn-secondary"%>
                <%if @article.valid?%>
                    <%= link_to "Delete this article", article_path(@article.id), method: :delete, data:{confirm: "Are you sure?"}, class:"btn btn-danger", style: "margin-left: 10px"%>
                <%end%>
            </div>
        </div>

    </div>

</div>
